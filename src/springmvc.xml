<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:vc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
            https://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/context
            https://www.springframework.org/schema/context/spring-context.xsd
            http://www.springframework.org/schema/mvc
            https://www.springframework.org/schema/mvc/spring-mvc.xsd">
    <context:component-scan base-package="com"/>
    <mvc:annotation-driven validator="validator"/>

    <!-- 输入校验 -->
    <!--校验器bean-->
    <bean id="validator" class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean">
        <!--注入HibernateValidatoer -->
        <property name="providerClass" value="org.hibernate.validator.HibernateValidator"/>
        <property name="validationMessageSource" ref="messageSource"/>
    </bean>
    <!--消息资源文件bean-->
    <bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <property name="basenames" value="classpath:msg"/>
        <property name="defaultEncoding" value="UTF-8"/>
        <property name="fileEncodings" value="UTF-8"/>
        <property name="cacheSeconds" value="100"/>
    </bean>
    <!--消息资源bean-->
    <bean class="org.springframework.context.support.ResourceBundleMessageSource"></bean>


    <!-- 配置拦截器 -->
    <!-- 拦截器都是基于HandlerMapping进行拦截的，可以将拦截器bean注入到映射器的interceptors属性中 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/regUser.do"/>
            <bean class="com.springmvc3.MyInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/regUser2.do"/>
            <bean class="com.springmvc3.MyInterceptor21"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/regUser2.do"/>
            <bean class="com.springmvc3.MyInterceptor22"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/*.do"/>
            <mvc:exclude-mapping path="/login.do"/>
            <bean class="com.springmvc3.LoginInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

</beans>